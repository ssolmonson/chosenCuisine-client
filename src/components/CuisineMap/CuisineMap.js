// Start of structure for the map component
// Plan to use Leaflet to map out the restaurant locations

// Consider map on restaurants page or just using map component within individual restaurant ids
// This could just be done when rendering each location

// Note: Still need to review and define how the address will be entered
// Look into if it's better to change how the address is stored in the database:
// {Address: Street, City, State, Zip} or pulled for each piece as it's setup right now.
